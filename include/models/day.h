#ifndef DAY_H
#define DAY_H
#include <stddef.h> 

typedef enum {
    DAY_INVALID = -1,
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY
} Day; // que valores puede tener un "day"
#endif